# multiplicação com lambda 2num

multi = lambda x,y: x * y

valor1 = int(input('Digite um número: '))
valor2 = int(input('Digite outro número: '))

print(f'O resultado de {valor1} * {valor2} é {multi(valor1, valor2)}')
